# Tilt Sensor with Arduino

## Purpose
Detect tilt or orientation changes.

## Connections
- Pin 1 → D2
- Pin 2 → GND

## Working
The tilt sensor changes its state when tilted. Arduino reads the sensor and displays the result on the Serial Monitor.

## Output
Tilt Detected
